import streamlit as st import json st.set_page_config(page_title="학칙 검색", layout="wide") st.title("국립공주대학교 학칙 검색 시스템") st.write("조문 또는 키워드를 입력하세요") # rules.json 불러오기 with open("rules.json", encoding="utf-8") as f: rules = json.load(f) # 검색 입력창 query = st.text_input("검색어 입력 (예: 제93조, 졸업, 휴학)") # 검색 함수 def search_rules(q): results = [] for r in rules: if q in r["조문"] or q in r["본문"]: results.append(r) return results # 검색 실행 if query: results = search_rules(query) if results: for r in results: st.write("------") st.write(f"■ {r['조문']} ({r['제목']})") st.write(r["본문"]) else: st.write("검색 결과 없음")